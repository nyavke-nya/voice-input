# Компактная CPU-база Windows-сборки (x64). NVIDIA DLL скачивает сам Setup.
# ponytail: нижние границы, а не жёсткие пины — после первого зелёного билда на
# чистой VM зафиксируй точные версии колёс, если что-то поедет.
sounddevice>=0.4.6
numpy>=1.24,<3
faster-whisper>=1.0.0
onnxruntime>=1.16          # CPU; НЕ onnxruntime-gpu (GPU не заявляем без проверки)
PySide6>=6.6
pynput>=1.7
pyinstaller>=6.6
