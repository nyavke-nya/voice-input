# Windows GPU-сборка: CPU-база + CUDA-рантайм (cuBLAS/cuDNN) для faster-whisper.
# Windows-колёса ctranslate2 поддерживают И CPU, И CUDA одним бинарником — нужен
# лишь рантайм CUDA рядом. device: "auto" сам берёт GPU при наличии NVIDIA, иначе
# молча падает на CPU, поэтому сборка остаётся универсальной.
#
# ВНИМАНИЕ: cuBLAS+cuDNN добавляют ~1.3 ГБ колёс (installer вырастет на ~1.5 ГБ).
# ctranslate2 4.8 ↔ CUDA 12 ↔ cuDNN 9 — версии согласованы, но не проверены на
# реальном NVIDIA-железе из этой среды (см. отчёт).
-r requirements-windows.txt
nvidia-cublas-cu12>=12.4
nvidia-cudnn-cu12>=9.1
